#include <stdio.h>

int main() {
    int n, i, val;
    float sum = 0;

    scanf("%d", &n);
    for(i = 0; i < n; i++) {
        scanf("%d", &val);
        sum += val;
    }

    printf("Array Mean Value is %.2f", sum / n);
    return 0;
}
